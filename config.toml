# The URL the site will be built for
base_url = "https://delaneycunningham.github.io"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

theme = "hallo"

[extra]
greeting = "Delaney Cunningham"

iam = "UX + Service Designer"

links = [
    { title = "Portfolio", url = "https://delaney.fyi", iconset = "fas", icon = "folder-open" },
    { title = "LinkedIn", url = "https://www.linkedin.com/in/delaneycunningham/", icon = "linkedin" }
]


[extra.theme]
background = "#2d3c40"
foreground = "#FFF"
hover = "#bbcdd2"
h1 = "2rem"
h2 = "1rem"
defaultsize = "14px"